<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent" android:layout_height="48dp"
	android:orientation="horizontal" android:background="@drawable/background">
	<ImageButton android:layout_width="40dp" android:layout_marginLeft="5dp"
		android:layout_height="40dp" android:layout_alignParentLeft="true"
		android:src="@drawable/add" android:cropToPadding="true"
		android:layout_centerVertical="true" 
		android:id="@+id/add" android:background="@drawable/button_background" 
		android:visibility="invisible">
	</ImageButton>
	<TextView android:layout_width="wrap_content"
		android:layout_height="wrap_content" android:text="@string/app_name"
		android:textColor="@color/white" android:textSize="18dp"
		android:layout_centerInParent="true" android:id="@+id/bartext"></TextView>
	<ImageButton android:layout_width="40dp"
		android:layout_height="40dp" android:layout_alignParentRight="true"
		android:src="@drawable/save" android:layout_centerVertical="true" 
		android:id="@+id/save" android:background="@drawable/button_background"
		android:layout_marginRight="5dp"
		android:visibility="invisible">
	</ImageButton>

    <ImageButton android:layout_width="40dp"
		android:layout_height="40dp" android:src="@drawable/trash"
		android:layout_centerVertical="true" android:id="@+id/sync"
		android:background="@drawable/button_background" android:layout_toLeftOf="@id/save"
		android:visibility="gone">
	</ImageButton>
</RelativeLayout>
